<nav class="navbar navbar-expand-lg px-4 py-3">
    <div class="container-fluid">
        <!-- Title -->
        <a class="navbar-brand fw-bold fs-3" href="#">
            My Dashboard
        </a>

        <!-- Navbar Collapse -->
        <div class="navbar-collapse collapse">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item dropdown">
                    <!-- Account Icon -->
                        <img src="~/images/account.png" class="avatar img-fluid rounded-circle me-2"
                             alt="User Avatar" style="width: 40px; height: 40px;" />
                        <span class="d-none d-md-inline text-dark">Account</span>
                    </a>

                    <!-- Dropdown Menu -->
                    <ul class="dropdown-menu dropdown-menu-end rounded shadow-sm">
                        <li>
                            <a href="@Url.Action("Profile", "AdminPanel")" class="dropdown-item">
                                <i class="lni lni-user me-2"></i> Profile
                            </a>
                        </li>
                        <li>
                            <a href="@Url.Action("Settings", "AdminPanel")" class="dropdown-item">
                                <i class="lni lni-cog me-2"></i> Settings
                            </a>
                        </li>
                        <li>
                            <hr class="dropdown-divider" />
                        </li>
                        <li>
                            <a href="@Url.Action("Logout", "Account")" class="dropdown-item text-danger">
                                <i class="lni lni-exit me-2"></i> Logout
                            </a>
                        </li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>
