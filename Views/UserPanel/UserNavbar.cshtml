@{
    string currentController = ViewContext.RouteData.Values["controller"]?.ToString();
    string currentAction = ViewContext.RouteData.Values["action"]?.ToString();
}

<nav class="navbar navbar-expand-lg" style="background-color: #052C65;">
    <div class="container">
        <a class="navbar-brand fw-bold text-white fs-2" href="@Url.Action("Index", "Home")">
            RNV
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
            <ul class="navbar-nav text-center">
                <li class="nav-item">
                    <a class="nav-link @(currentController == "UserPanel" && currentAction == "HomePage" ? "active" : "") text-white" href="@Url.Action("HomePage", "UserPanel")">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(currentController == "UserPanel" && currentAction == "Maintenance" ? "active" : "") text-white" href="@Url.Action("Maintenance", "UserPanel")">Maintenance</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(currentController == "UserPanel" && currentAction == "Complain" ? "active" : "") text-white" href="@Url.Action("Complain", "UserPanel")">Complain</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(currentController == "UserPanel" && currentAction == "Booking" ? "active" : "") text-white" href="@Url.Action("Booking", "UserPanel")">Booking</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(currentController == "UserPanel" && currentAction == "NoticeBoard" ? "active" : "") text-white" href="@Url.Action("NoticeBoard", "UserPanel")">Notice Board</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(currentController == "UserPanel" && currentAction == "AddVisitor" ? "active" : "") text-white" href="@Url.Action("AddVisitor", "UserPanel")">Add Visitor</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(currentController == "UserPanel" && currentAction == "VehicleInfo" ? "active" : "") text-white" href="@Url.Action("VehicleInfo", "UserPanel")">Vehicle Info</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
